import { ROUTING } from '../constants/constants';

const MENU_OPTIONS = [
  {
    'name': 'HOME',
    'path': ROUTING.HOME
  }
];

const MENU_TITLE = "Comercialización";

export {
  MENU_OPTIONS,
  MENU_TITLE
};
