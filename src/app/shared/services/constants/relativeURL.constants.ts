const rootPath = '/fomento';

const RELATIVE_PATH = {
  SIGN_UP: `${rootPath}/user/signup`,
  LOGIN: `${rootPath}/login`,
  GET_USER: `${rootPath}/user`,
  EMAIL_VERIFICATION: `${rootPath}/user/verify`,

  GET_SPECIFIC_CONDITIONS: `${rootPath}/specific_condition`,
  GET_AGE_RANGE: `${rootPath}/age_range`,
};

const AUTHORIZATION = 'Authorization';

export {
  RELATIVE_PATH,
  AUTHORIZATION
};

